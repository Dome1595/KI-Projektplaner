# Supabase (EU-Projekt) – ohne diese Werte läuft die App im Mock-Betrieb
# (localStorage mit Beispieldaten). Werte: Supabase-Dashboard → Project
# Settings → API. Kopie als .env.local anlegen (wird nicht eingecheckt).
NEXT_PUBLIC_SUPABASE_URL=https://naallvmyawfmkqzrlxie.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Nur serverseitig verwendet (Server Actions); niemals ins Client-Bundle.
SUPABASE_SERVICE_ROLE_KEY=
